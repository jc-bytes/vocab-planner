# Workspace Tools

- `planning/` contains repeatable curriculum, rubric, vocabulary, document-formatting, and school-sync utilities.
- `analysis/` contains targeted extraction or inspection utilities that are not part of the normal planning workflow.

Planning scripts locate the workspace from their own file path, so they can be launched from the workspace root or another working directory.
