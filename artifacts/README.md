# Artifacts

This folder holds outputs rather than source material.

- `generated/` contains deliverables and previews produced by scripts.
- `backups/` contains local safety copies and is ignored by Git.
- `temporary/` contains disposable working files and is ignored by Git.
