# Standalone Sites

Student-facing sites that are not yet registered as reusable learning modules live here. When a site becomes reusable across lessons, migrate it into `learning-modules/`, add it to `learning-modules/module-catalog.json`, and preserve its provenance.
